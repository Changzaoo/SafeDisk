services:
  - type: web
    name: safedisk-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm run start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 24.15.0
      - key: FRONTEND_ORIGIN
        sync: false
      - key: SAFEDISK_DB_PATH
        value: /tmp/safedisk.sqlite
      - key: SAFEDISK_LOG_DIR
        value: /tmp/safedisk-logs
